# oldsrc 只读归档

本目录保存迁移前实现，仅用于协议、算法和界面行为考古，不参与 CMake 构建。

现代实现与测试的最终映射见 [`docs/legacy-capability-map.md`](../docs/legacy-capability-map.md)。新功能和缺陷修复必须进入 `include/dss/`、`src/` 与 `tests/`，不得继续维护本目录中的生产代码。